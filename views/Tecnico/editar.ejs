<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Técnico</title>
</head>
<body>
    <h1>Editar Técnico</h1>
    <form action="/editTecnico" method="post">
        <label for="id">Codigo</label>
        <input type="text" name="id" id="id" value="<%= tecnico.id %>" readonly>
        <br><br>
        <label for="nombre_tecnico">Nombre</label>
        <input type="text" name="nombre_tecnico" id="nombre_tecnico"  value="<%= tecnico.nombre_tecnico %>"required>
        <br><br>
        <label for="email">Email</label>
        <input type="text" name="email" id="email" value="<%= tecnico.email %>" required>
        <br><br>
        <label for="telefono">Telefono</label>
        <input type="text" name="telefono" id="telefono" value="<%= tecnico.telefono %>" required>
        <br><br>
        <label for="ID_Rol">Rol</label>
        <select name="ID_Rol" required>
            <% rol.forEach((dato) => { %>
                <option value="<%= dato.id %>" <%= tecnico.ID_Rol == dato.id ? 'selected' : '' %>>
                    <%= dato.Nombre_rol %>
                </option>
            <% }) %>        
        </select>
        <br><br>
        <button type="submit">Guardar Cambios</button>
        <button onclick="window.location.href='/tecnico'">Volver</button>
    </form>
</body>
</html>